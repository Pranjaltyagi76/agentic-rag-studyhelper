[pytest]
testpaths = tests
addopts = -q
# Keep noise low: the app's third-party deps emit sunset/deprecation warnings that are
# unrelated to the tests. The msgpack-registration fix is asserted explicitly instead.
filterwarnings =
    ignore::DeprecationWarning
