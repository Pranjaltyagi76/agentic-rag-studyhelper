# Agentic RAG Study Helper — curated dependencies.
#
# This replaces the original global `pip freeze` dump, which was bloated
# (streamlit/jupyter/kubernetes/…), missing packages the code imports, and pinned
# tokenizers/huggingface_hub in a way that conflicted with sentence-transformers.
#
# Embeddings run on fastembed (ONNX) instead of sentence-transformers (PyTorch):
# same model (all-MiniLM-L6-v2, 384-dim), ~15 MB vs ~2.5 GB, no torch — much lighter
# for the free HF Spaces deploy. See strategy.md decision log.
#
# Versions are intentionally unpinned so pip resolves a mutually compatible set on
# Python 3.13. After a successful install, run `pip freeze > requirements.lock.txt`
# to capture exact versions for reproducible/prod builds.

# --- Web / API ---
fastapi
uvicorn[standard]
python-multipart
pydantic

# --- Config ---
python-dotenv

# --- Agent / LangGraph ---
langgraph
langchain-core
langchain-community
langchain-text-splitters
langchain-groq
langchain-google-genai
langchain-tavily
langchain-chroma

# --- Vector store + embeddings (torch-free) ---
chromadb                       # local dev backend
langchain-postgres             # pgvector backend (deploy / docker-compose)
fastembed

# --- Ingestion ---
pypdf
google-genai

# --- Observability (LangSmith comes with langchain; add OTel FastAPI instrumentation) ---
opentelemetry-instrumentation-fastapi

# --- Evaluation (Phase 8.5): offline RAG quality scoring, logged to MLflow (local) ---
mlflow

# --- Persistence ---
SQLAlchemy
alembic                        # schema migrations (sessions/documents tables)
psycopg2-binary
# LangGraph checkpointers: SQLite for local dev, Postgres (Neon) for deploy.
langgraph-checkpoint-sqlite
langgraph-checkpoint-postgres
psycopg[binary]                # driver PostgresSaver needs (psycopg v3); used in Phase 9
